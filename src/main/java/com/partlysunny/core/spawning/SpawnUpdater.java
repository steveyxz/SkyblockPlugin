package com.partlysunny.core.spawning;

import com.partlysunny.core.sql.SQLManager;
import org.bukkit.Server;
import org.bukkit.event.inventory.InventoryClickEvent;

public class SpawnUpdater {

    private static final SQLManager manager = new SQLManager("/spawnData.db");

    public static void initializeSpawnBlocks(Server s) {
    }


}
